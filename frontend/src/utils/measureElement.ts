import React from 'react';
import ReactDOM from 'react-dom';

export const containerStyle = {
	display: "inline-block",
	position: "absolute",
	visibility: "hidden",
	zIndex: -1
};

const measureLayer = document.getElementById("me")